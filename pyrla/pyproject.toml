[project]
name = "pyrla"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "httpx>=0.28.1",
    "tenacity>=9.0.0",
    "typer>=0.16.1",
]
[project.scripts]
pyrla = "pyrla.cli:app"

[tool.uv]
package = true
